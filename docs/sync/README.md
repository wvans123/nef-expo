# 内网变更同步记录

分类：报告 / 快照。更新：2026-09-20。

来源是用户提供的 2026-09-18 17:30 文字说明，另一台机器基线为 `b4115e6`。本机从 `7a6a8bc` 同步，保留后来新增的“发布 NEF 目录”选择、预览、发布入口。

此次按行为重建，未收到 `FULL-DIFF-since-b4115e6.patch`，因此不宣称与另一台逐行一致，也不复刻其临时补丁生成脚本或四份历史快照。

| 范围 | 同步结果 |
|---|---|
| 场景回传 | 无 Key POST/GET、固定共享通道、备用接收 Key、私有通道隔离 |
| 机器狗 | text/plain Intent、GET/POST 结果拉取、3 秒页面轮询、去重与失败不影响 Intent |
| 订购 | 所有套餐通知、过滤范围、8 折按所选能力计价、取消 DELETE、2053/2051、请求回包记录 |
| 农场 MCP | 无 Key 一步登记/发现/发布、同 URL 更新、开放登记跨账号可见 |
| 页面 | 价格、取消开通、ops-only、实例变化清账号、文字/数据结果；移除媒体查看器 |
| 本机新增功能 | 选定 NEF 目录发布入口继续保留 |

明确差异：启动自动删除农场套餐改为 `reset_partner_plans_on_start` 显式开关，默认 false；公开模板的现场 URL 留空，文档只保留地址占位符、端口和接口路径，本机 local 配置、模型凭据与运行服务未修改。后端继续兼容空能力选择，页面新订购至少选一项。

本地验证使用隔离配置和模拟 TCP 对端，不能替代真实农场、机器狗或目录验收。用户提供的另一台测试数量仅作为来源信息，不作为本机测试结果。

本机验证：83 项改动相关 Python 测试及 61 项网关、农场、场景、回传、目录、查询回归通过，未跑全量；purchase 与 showcase-story 两组 Node 测试通过。Playwright 在 1440×1000、390×844 验证订购报价、取消、匿名回传、结果拉取、过期响应、实例变化清账号，并回归原目录选择发布；截图经查看。临时服务只使用本地模拟对端与隔离配置，不触碰 8069。

2026-09-20 发布前复验：83 项后端定向测试通过；发布模板调整后另跑 21 项配置/页面契约测试（含新增 3 个无地址不外联用例，与前一轮有重叠），5 组 Node 脚本通过。两个浏览器脚本均在桌面/手机尺寸通过。新增验证实际定时拉取、结构化数据显示、失败不覆盖 Intent 回执，并复现、修复了历史裁剪后旧文字/数据残留。未跑全量后端，未访问真实伙伴。

用户授权上传现有 GitHub 仓库。发布检查确认 local 配置、环境文件、运行日志和截图不进入提交；公开文档中的现场 IP 改为占位符，模板默认不连接业务服务。该操作不更新已运行的 Python 进程，也不迁移内存数据。

本次同步维护的文档：根 README、docs/README 索引、demo-playbook、reference 下的 integration / subscription-query / farm-integration / manual-curl，以及两个现行展示 / 鉴权设计；本记录作为同步快照。未生成未收到的原始累计 patch，发布状态以 Git 提交和远程分支为准。

日常入口以 [README](../../README.md) 为准；接口与命令分别维护在 [场景接口](../reference/integration.md)、[订购通知](../reference/subscription-query.md)、[农场 MCP](../reference/farm-integration.md)、[curl 手册](../reference/manual-curl.md)，不在本记录重复定义契约。
